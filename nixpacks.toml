[phases.setup]
# "..." keeps any Nixpacks-detected packages; python3 ensures pip exists.
nixPkgs = ["...", "python3"]

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
